#pragma once

#include <RandomEngine/API/Math/vec2.hpp>

using namespace random_engine;

namespace 
{
	constexpr float Y_UNIT = 1.f / 7.f;
	constexpr float X_UNIT = 1.f / 10.f;
	const vec2 UNIT = { X_UNIT, Y_UNIT };
}